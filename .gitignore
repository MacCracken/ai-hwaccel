# Build artifacts
/build/

# Vendored stdlib — repopulated by `cyrius deps` from the
# version-pinned snapshot in cyrius.cyml [deps] stdlib. Keeping
# this out of the repo prevents stale stubs from a prior cyrius
# version sitting in tree (matches agnosys/libro/patra convention).
/lib/

# Old Rust crate (archived, not tracked)
rust-old/

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Debug
*.pdb
*.so
*.dylib
*.dll
core
core.*
qemu_*.core

# Coverage
lcov.info
tarpaulin-report.html
coverage/
criterion/

# Secrets / environment
.env
.env.*

# Claude Code
.claude/
