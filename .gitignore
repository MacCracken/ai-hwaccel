# Build artifacts
/build/

# Vendored stdlib — repopulated by `cyrius deps` from the
# version-pinned snapshot in cyrius.cyml [deps] stdlib. Keeping
# this out of the repo prevents stale stubs from a prior cyrius
# version sitting in tree (matches agnosys/libro/patra convention).
/lib/

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Debug
*.pdb
*.so
*.dylib
*.dll
core
core.*
qemu_*.core

# Coverage
lcov.info
coverage/

# Python bindings (bindings/python/)
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
bindings/python/dist/
bindings/python/build/
# Per-platform binary is injected into the wheel at build time (2.3.3),
# not committed.
bindings/python/src/ai_hwaccel/_bin/

# Secrets / environment
.env
.env.*

# Claude Code
.claude/
